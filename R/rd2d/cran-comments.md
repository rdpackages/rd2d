# rd2d 1.0.0 CRAN comments

## Test environments

- Local Windows 11 install, R 4.6.0
- GitHub Actions: ubuntu-latest, R 4.6.0

## R CMD check results

0 errors | 0 warnings | 2 notes

The CRAN incoming NOTE reports that the package was updated recently. This is
a major feature and numerical-alignment release across the R, Python, and Stata
implementations.

The local HTML manual NOTE reports that HTML validation and math rendering were
skipped because the local Windows machine does not have `tidy` and the R
package `V8` available.
